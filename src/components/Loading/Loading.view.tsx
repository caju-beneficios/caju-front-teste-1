import { LoadingBackground, LoadingSpinner } from "./Loading.styles";

export default function LoadigView() {
  return (
    <LoadingBackground>
      <LoadingSpinner />
    </LoadingBackground>
  );
}
